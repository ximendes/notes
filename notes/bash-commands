# Bash Commands

Create a File

```
$ touch [file_name]
```

Create a Directory

```
$ mkdir [folder_name]
```

Search Text

```
$ grep [pattern file]
```

Describe what a command does

```
$ man [bash_command] 
```

Return the working directory name

```
$ pwd
```

Move or rename directory

```
$ mv
```

Copy files and dicrectories

```
$ cp
```

Example: to move a file named sample1.txt from the current directory to ~/Document directory, the command would be:

```
$ mv sample1.txt ~/Documents/

```

Show environment variables

```
$ printenv
```